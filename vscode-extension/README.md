# Critiqor for AI Agents

Install the extension in VS Code or Cursor, then run **Critiqor: Configure Agent** from the Command Palette.

Requires the `critiqor` Python package to be installed and available on `PATH`.
